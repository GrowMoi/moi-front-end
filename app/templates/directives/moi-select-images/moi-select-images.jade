.row
  span.title-grid-images
    | {{ 'image.secret' | translate }}
.row.grid-images
  .col.col-20(ng-repeat="image in vmSelectImages.images" ng-click="vmSelectImages.onSelect(image.key)")
    img(ng-src='{{image.path}}' ng-class="{selected: image.selected}")
    span
      | {{image.name}}
